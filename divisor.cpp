#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
    string godlike[2] = {"s", "s"};
    for(int i = godlike.length() - 1; i > 0; i--) {
        cout << i;
    }
}
